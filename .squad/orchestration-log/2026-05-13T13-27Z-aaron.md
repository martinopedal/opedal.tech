# Orchestration: Aaron — CV Pipeline Specialist

**Session:** PR B execution (`feat/cv-and-redesign`)  
**Date:** 2026-05-13T13:27Z  
**Role:** ⚙️ CV Pipeline Specialist

## Why Aaron was chosen

PR B includes a complete CV build pipeline overhaul: replacing broken `xu-cheng/latex-action` with apt texlive + Python orchestration in `cv.yml`. Aaron owns the LaTeX → PDF build.

## Mode

Background (sub-task spawn). Coordinate routed emergency fixes to Aaron's surface (see coordinator-bypass entry below).

## Files produced

- `.github/workflows/cv.yml` — new CV build pipeline with texlive setup and PDF commit-back
- `cv/build.py` — Python orchestration for latexmk + Jinja2 rendering
- `cv/templates/architect.tex.j2` — Jinja2-templated LaTeX with custom delimiters
- `cv/data/architect.yml` — sanitized YAML source (email alias, phone removed, sales-language softened)
- `.squad/agents/aaron/history.md` — cross-agent updates (appended)
- Decisions in `.squad/decisions/inbox/` (3 items: latex-action, commit-back, jinja-delimiters)

## Outcome

✅ **Build CV PDFs passes.** Commit `1b56afb` landed the core pipeline. Three emergency fixes (commits `c688775`, `f144c32`, `39c0009`) unblocked CI:
1. hyperref option clash via PassOptionsToPackage
2. unclosed \bgupper brace in \header invocation
3. LaTeX special char escaping via Jinja finalize filter

All decisions (LaTeX action choice, commit-back strategy, Jinja custom delimiters) documented in inbox for merge into decisions.md.
